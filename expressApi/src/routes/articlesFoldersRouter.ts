// import {Router} from 'express'
// import * as authController from '../controllers/authController/authController'
// import * as articlesFoldersController
//     from '../controllers/articlesFoldersController/articlesFoldersController'


// const router = Router()

// router.route('/:siteId')
    // Получение порядка расположения шаблонов компонентов определённого сайта
    // .get(authController.protect, articlesFoldersController.getFolders)
    // Изменение порядка расположения шаблонов компонентов определённого сайта
    // .put(authController.protect, articlesFoldersController.setFolders)


// export default router